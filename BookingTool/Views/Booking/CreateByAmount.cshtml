@model BookingTool.Models.Booking

@{
    ViewBag.Title = "Create";
}

<h2>Buchung erstellen | Einzelbuchung</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Booking</legend>        

        <div>
            @Html.LabelFor(model => model.Name)
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <div>
            @Html.LabelFor(model => model.Description)
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>

        <div>
            @Html.LabelFor(model => model.DateBooked)
@*            @Html.TextBox("DateBookedUtc", Model.DateBooked.ToString("dd.MM.yyyy"))*@
            @Html.EditorFor(model => model.DateBooked)
            @Html.ValidationMessageFor(model => model.DateBooked)
        </div>
        
        <div>
            <table>
                <tr>
                    <th>Sender</th>
                    <th>Empfänger</th>
                    <th>Betrag</th>
                </tr>

                @for(var i = 0; i < ViewBag.ParticipantsCount; i++)
                {
                    @Html.EditorFor(model => model.PartialBookings[i])
                }
            </table>
        </div>

        <p>
            <input type="submit" value="Erstellen" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Zurück zum Kontostand", "MyAccount")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
